{
  "name": "@luncheon/fluentpath",
  "version": "0.0.1",
  "description": "SVG freehand smooth drawing.",
  "main": "index.js",
  "prettier": {
    "printWidth": 140,
    "endOfLine": "lf",
    "singleQuote": true,
    "trailingComma": "all"
  },
  "scripts": {
    "start": "esbuild --bundle --minify --outfile=index.js index.ts --serve --servedir=. --sourcemap",
    "build": "esbuild --bundle --minify --outfile=index.js index.ts"
  },
  "devDependencies": {
    "esbuild": "^0.14.39",
    "prettier": "^2.6.2",
    "typescript": "^4.6.4"
  }
}
